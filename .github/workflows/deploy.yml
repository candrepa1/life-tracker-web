name: deploy
on: [push]
jobs:
  push-to-s3:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install
      - run: npm run build
      - run: aws s3 sync ./dist s3://${{secrets.BUCKETNAME}}
